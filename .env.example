# Optional: AgentSmith Connect source builds
# `cp .env.example .env` enables AgentSmith Connect against the production deployment.
# These are the same public identifiers baked into official release builds, not
# secrets. Remove or comment them out to build with cloud features disabled.
# Do not add server-side secrets to this file.

# Production Clerk instance. To use your own, get these from the Clerk Dashboard
# under API keys, JWT templates, and OAuth applications.
AGENTSMITH_CLERK_PUBLISHABLE_KEY=pk_live_Y2xlcmsudDMuY29kZXMk
AGENTSMITH_CLERK_JWT_TEMPLATE=agentsmith-relay
AGENTSMITH_CLERK_CLI_OAUTH_CLIENT_ID=hzxSgY2cH10sDU2r

# Optional: signed macOS passkey builds. The RP domain defaults to the Frontend API
# hostname encoded in AGENTSMITH_CLERK_PUBLISHABLE_KEY. Set the override only when Clerk
# returns a different RP ID or when multiple domains must be entitled.
# AGENTSMITH_APPLE_TEAM_ID=ABC1234567
# AGENTSMITH_MACOS_PROVISIONING_PROFILE=/absolute/path/to/agentsmith.provisionprofile
# AGENTSMITH_CLERK_PASSKEY_RP_DOMAINS=example.clerk.accounts.dev,clerk.example.com

# Production relay. For a self-hosted relay, `infra/relay` deploys update it
# automatically.
AGENTSMITH_RELAY_URL=https://relay.agentsmith.dev

# Optional: hosted app origin used by the CLI's out-of-band OAuth flow.
# Defaults to https://app.agentsmith.dev; override to test against a staging deployment.
# AGENTSMITH_HOSTED_APP_URL=https://nightly.app.agentsmith.dev

# Public, ingest-only mobile OpenTelemetry configuration.
# AGENTSMITH_MOBILE_OTLP_TRACES_URL=https://api.axiom.co/v1/traces
# AGENTSMITH_MOBILE_OTLP_TRACES_DATASET=agentsmith-mobile-traces-dev
# AGENTSMITH_MOBILE_OTLP_TRACES_TOKEN=xaat-...
